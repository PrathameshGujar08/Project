import React from 'react';
interface grp{
    x:number,
    y:number,
    scale:number,
    value:string,
    id:string
}
const CustomGroup:React.FC<grp> = ({ x, y, scale,value,id }) => {
  return (
    <></>
  );
};

export default CustomGroup;
